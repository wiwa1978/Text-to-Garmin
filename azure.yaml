# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: text-to-garmin
metadata:
  template: text-to-garmin@0.1.0

services:
  web:
    project: .
    host: containerapp
    language: python
    docker:
      path: Dockerfile
      context: .
